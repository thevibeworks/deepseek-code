// Mobile client class. Devices are considered higher-theft-risk, so the
// refresh cap is tighter than the base policy (security review 2026-01).
const REFRESH_CAP_DAYS = 21;

module.exports = { REFRESH_CAP_DAYS };
