// Web client classes. Standard web uses the base policy; the opt-in
// "remember this device" flow uses the extended lifetime below.
const EXTENDED_REFRESH_DAYS = 90;

module.exports = { EXTENDED_REFRESH_DAYS };
